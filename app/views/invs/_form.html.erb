<%= form_for(@inv, html: {multipart: true}) do |f| %>
  <% if @inv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inv.errors.count, "error") %> prohibited this inv from being saved:</h2>

      <ul>
      <% @inv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
<% end %>

<div class="field">
	 <%= f.label :image%><br>
    <%= f.file_field :image%>
</div>

  <div class="field">
    <%= f.label :inv_desc %><br>
    <%= f.text_field :inv_desc %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
