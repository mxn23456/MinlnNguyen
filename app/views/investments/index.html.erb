<p id="notice"><%= notice %></p>

<h1>Listing Investments</h1>

<table>
  <thead>
    <tr>
      <th>Investment desc</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @investments.each do |investment| %>
      <tr>
        <td><%= investment.investment_desc %></td>
        <td><%= investment.notes %></td>
        <td><%= link_to 'Show', investment %></td>
        <td><%= link_to 'Edit', edit_investment_path(investment) %></td>
		<td><%= link_to 'Destroy', investment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		<td><%= link_to 'Transactions', investment_investment_transactions_path(investment) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Investment', new_investment_path %>
